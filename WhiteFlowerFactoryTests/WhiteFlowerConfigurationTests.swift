//
//  WhiteFlowerConfigurationTests.swift
//  WhiteFlowerFactoryTests
//
//  Created by Stephen Bodnar on 4/26/20.
//  Copyright © 2020 Stephen Bodnar. All rights reserved.
//

import XCTest

class WhiteFlowerConfigurationTests: XCTestCase {
    
    /*func testDefaultInit() {
        let configuration: WhiteFlowerConfiguration = WhiteFlowerConfiguration.default
        
        XCTAssertTrue(configuration.dispatchQueue == DispatchQueue.main)
        XCTAssertTrue(configuration.defaultHeaders.count == 0)
        XCTAssertTrue(configuration.session.configuration == URLSessionConfiguration.default)
    }
    
    func testInitWithParams() {
        let queue: DispatchQueue = DispatchQueue(label: "test")
        let headers: [HTTPHeader] = [HTTPHeader(field: HTTPHeaderField.authorization, value: "Bearer abc123"), HTTPHeader(field: HTTPHeaderField.userAgent, value: "macbook-safari")]
        let configuration: WhiteFlowerConfiguration = WhiteFlowerConfiguration(dispatchQueue: queue, defaultHeaders: headers, session: URLSession(configuration: URLSessionConfiguration.default))
        
        XCTAssertTrue(configuration.session.configuration.httpAdditionalHeaders!.count == 2)
        XCTAssertTrue(configuration.dispatchQueue.label == "test")
    }*/
}

